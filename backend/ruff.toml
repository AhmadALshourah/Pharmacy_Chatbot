[lint]
# E402: module-level import not at top of file.
# main.py and ingest.py deliberately call load_dotenv() / API-key check
# before importing LangChain/OpenAI modules, so this rule is suppressed there.
per-file-ignores = { "app/main.py" = ["E402"], "app/ingest.py" = ["E402"] }
